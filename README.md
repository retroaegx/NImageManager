# NImage Manager

## Python のインストール

このアプリは Python 3.12 または 3.13 を使用します。  
新規導入は **Python 3.13.12** を推奨します。

先に Python 3.13 が入っているか確認してください。

- Windows: コマンドプロンプトを開き、`py -3.13 --version` を実行する。
- macOS / Linux: ターミナルを開き、`python3.13 --version` を実行する。

入っていなければ、下記で **Python 3.13.12** を入れてください。

- Windows: 公式の Python 3.13.12 配布ページからインストーラーを入手してインストール  
  https://www.python.org/downloads/release/python-31312/
- macOS (Homebrew): `brew install python@3.13`
- Ubuntu / Debian: 公式ソース配布物から Python 3.13.12 をインストール

```bash
sudo apt update
sudo apt install -y build-essential wget libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev libncursesw5-dev libffi-dev libgdbm-dev liblzma-dev tk-dev uuid-dev
cd /tmp
wget https://www.python.org/ftp/python/3.13.12/Python-3.13.12.tgz
tar -xzf Python-3.13.12.tgz
cd Python-3.13.12
./configure --enable-optimizations
make -j"$(nproc)"
sudo make altinstall
```

インストール後は次で確認してください。

- Windows: `py -3.13 --version`
- macOS / Linux: `python3.13 --version`

`Python 3.13.12` と表示されれば問題ありません。  
Windows はインストール時に `Add Python to PATH` を ON にしてください。  
このアプリは起動時に **Python 3.13 を優先し、なければ 3.12 を使用**して仮想環境を作成します。  
**Python 3.12 / 3.13 がどちらも入っていない場合はエラーで起動を停止します。**


## 起動
- Windows: `run.bat`
- macOS/Linux: `sh run.sh`

※ 後続の作業の前に、初回はまず一度起動してください。  
※ 初回起動時は Python パッケージの導入や初期ファイル作成のため、少し時間がかかる場合があります。  

起動後にコンソールへ URL が表示されます。
- Local: `http://localhost:32287`
- Tunnel: `https://...trycloudflare.com`（有効な場合）※  
※ Cloudflare の Quick Tunnel はテスト・開発目的に限定されるサービスであるため、恒常的に使用することは許可されていません。  
テスト目的が終了している場合は、必ず公開アドレスの Cloudflare を固定接続 / ドメイン移管するか、非公開化を行ってください。

まずは起動した PC 自身のブラウザで `Local` の URL を開いてください。  
同じネットワーク（自宅内）の別の PC やスマートフォンからアクセスしたい場合は、起動した PC のローカル IP アドレスを調べて、`http://起動PCのIPアドレス:32287` を開きます。  
例: `http://192.168.1.25:32287`

### 起動した PC のローカル IP アドレスの調べ方

#### Windows
1. `Win + R` を押して `cmd` と入力し、Enter を押します。
2. `ipconfig` を実行します。
3. 使用中のネットワークアダプター（Wi-Fi または Ethernet）の `IPv4 Address` / `IPv4 アドレス` を確認します。
4. 例として `192.168.1.25` と表示された場合、アクセス先は `http://192.168.1.25:32287` です。


#### macOS
1. 「システム設定」→「Wi‑Fi」または「ネットワーク」から、現在接続中のネットワーク詳細を開くと IP アドレスを確認できます。


#### Linux
1. ターミナルを開きます。
2. 次のどちらかを実行します。  
   - `hostname -I`
   - `ip addr`
3. `192.168.x.x` や `10.x.x.x` のような、現在の LAN 内アドレスを確認します。
4. 例として `192.168.1.25` なら、アクセス先は `http://192.168.1.25:32287` です。

補足:
- `127.0.0.1` は自分自身専用のアドレスなので、別端末からは使えません。
- 複数のアドレスが表示された場合は、Wi-Fi / 有線 LAN で現在使っているものを選んでください。


### 別端末から接続できないときの確認項目
- 別端末が起動 PC と同じ Wi‑Fi / 同じローカルネットワークに接続されているか
- ブラウザで開く URL が `http://起動PCのIPアドレス:32287` になっているか
- 起動 PC 側のファイアウォールで 32287 番ポートの通信が遮断されていないか
- 起動後のコンソールにエラーが出ていないか


### IP アドレス固定化について
家庭内ネットワークでは、起動 PC のローカル IP アドレスが再起動や時間経過で変わることがあります。  
毎回同じアドレスで使いたい場合は、ルーター側の DHCP 固定割り当て（IP 予約）を設定してください。  やり方は環境によって異なるので自分で調べてください。


## 初期ログイン
起動後に アカウント名とパスワードを入力して作成します。  
マスターアカウント扱いでこのアカウントでしか出来ないことが複数あるので、パスワードを忘れないように気を付けてください。  


## 公開用アドレスを非公開アドレスに設定(ローカルネットワーク・PCのみで使用する場合)

ローカルネットワーク・PCのみで使用する場合、  
`.env`(初回起動後に作成されるファイル) の `NAI_IM_PUBLIC_BASE_URL=` の後ろに「`http://localhost:32287`」  
と記載してください。


## 公開用アドレスを固定する(スマトフォンや出先、他人に共有して使用できるようにする場合)

デフォルトだと Quick Tunnel で公開用アドレスを用意してくれますが、起動時に毎回アドレスが変わります。  
下記の内容はCloudflare Tunnels + 無料のDDNSを活用してアドレスを固定する例です。  
完全に独自ドメインを扱うやり方は環境によって異なるので、必要に応じて調べて対応してください。  

### 1. DDNS Now（ddns.kuku.lu）のトップへ行きドメインを登録する
[DDNS Now](ddns.kuku.lu)へアクセスし、左メニューの新規登録で

- 希望サブドメイン：****（英数と `-` など）
- パスワード：6文字以上

で登録します。  


### 2. Cloudflare にドメイン移管する
- [Cloudflareのトップ](https://dash.cloudflare.com/login)へ行きログイン（必要なら登録）する。
- ログイン後、ダッシュボード（左上のアドレスをクリック）> ドメインを選択し、右ペインから「+ドメインのオンボード」を選択
- 「既存のドメインを入力」に登録したアドレスを入力し、「続行」を押下する。
- プラン選択画面で Free を選択する。
- 「DNS レコードを確認する」画面で「アクティベーションを続行」を押下する。
- 「現在のネームサーバーを Cloudflare ネームサーバーに置き換える」の欄に表示されているDNSネームサーバーのアドレス2つをメモする。
- [DDNS Now の設定画面](https://ddns.kuku.lu/control.php)を開く。
- NSレコード欄のすぐ下にある「認証する」ボタンから電話認証してください。自身の電話番号入力して、進んだ先に表示される電話番号に自分の電話番号から発信、3秒後切断するだけです。
- 再び [DDNS Now の設定画面](https://ddns.kuku.lu/control.php)を開く。
- A/AAAA/CNAME/サブドメイン/ワイルドカードを全部空にし、下のほうにある「設定を更新する」ボタンを押下する。
- NSレコード欄に先ほどメモした2つのアドレスを入力（改行して入力すること）し、下のほうにある「設定を更新する」ボタンを押下する。
  
再び設定画面にリダイレクトされれば完了です。


### 3. パソコンのトンネル接続恒常化
- [Cloudflareのトップ](https://dash.cloudflare.com/login)へ行きログインする。
- Cloudflareのドメイン > 右ペインのドメイン名 > 画面右側の「DNS レコード」を開きます。`nim-sample.f5.si` の DNS 管理の中にチェックボックスのあるレコードが"あれば"、チェックを入れてレコードの削除を行ってください。
- ログイン後、ダッシュボード（左上のアドレスをクリック）> ネットワーク > Tunnels を選択し、右ペインから「トンネルを作成」ボタンを押下
- 任意のトンネル名を入力し、「トンネルを作成」を押下する。
- インストールと実行欄に表示されている手順に従い作業してください。一番下の「接続状態」に「トンネルが正常に接続されました」と表示されれば完了です。表示される msi をインストールし、コマンドプロンプトを管理者権限で実行、表示されているコマンドを実行するだけです。Ubuntuで環境構築したい場合は Debian の手順で実行してください。完了したら「続行」ボタンを押下してください。
- ダッシュボード（左上のアドレスをクリック）> ネットワーク > Tunnels を選択し、右ペインから作成したトンネル名を選択してください。
- 画面タブの Routes 欄の中にある「+ Add route」ボタンを押下
- 「Published application」を押下
- 表示されたフォームで右上の「Domain」に登録したドメインが表示されていることを確認し、「Service URL*」の欄に `http://127.0.0.1:32287` を入力し、「Add Route」ボタンを押下する。

あとは登録したアドレス（`https://xxxx.f5.si`）にブラウザでアクセスしてみてください。  
`.env`(初回起動後に作成されるファイル) の `NAI_IM_PUBLIC_BASE_URL=` の後ろに上のアドレスを記載してください。起動時にそのアドレスを表示してくれます。  
アカウントの作成やパスワードリセット時にこの値を使用するため間違えないように必ず記載してください。  


## NovelAI拡張機能追加
NovelAIの画面に転送登録用のボタンを追加する拡張機能の入れ方です。


### NovelAI拡張機能 PC版 Chrome の拡張機能の入れ方  
#### AまたはBで入れてください。    
A.下記のURLを開き、拡張機能のインストールを行ってください。  
https://chromewebstore.google.com/detail/nim-transfer/cnmickkcdgahfcjehhhnokkheaifadci?authuser=0&hl=ja
  
B.下記の手順に従いインストールしてください。  
1. Chrome で `chrome://extensions` を開きます。
2. 右上の「デベロッパー モード」を有効にします。
3. 「パッケージ化されていない拡張機能を読み込む」を押します。
4. `extensions/chrome/` フォルダを選びます。


### NovelAI拡張機能(ボタンによる自動登録) PC版 Firefox の拡張機能の入れ方
#### AまたはBで入れてください。  
A.下記のURLを開き、拡張機能のインストールを行ってください。  
https://addons.mozilla.org/ja/firefox/addon/nim-transfer/  

B.下記の手順に従いインストールしてください。  
1. `extensions/nim_firefox_addons.xpi` を用意します。  
2. Firefox の設定 - 拡張機能 - ⚙ - ファイルから拡張機能をインストールよりファイルとして開きます。
3. 確認ダイアログで追加します。


### Android Firefox の入れ方
#### AまたはBで入れてください。  
A.下記のURLを開き、拡張機能のインストールを行ってください。  
https://addons.mozilla.org/ja/firefox/addon/nim-transfer/  

B.下記の手順に従いインストールしてください。  
1. `extensions/nim_firefox_addons.xpi` を端末に保存します。
2. Android Firefox で「設定」→「Firefox について」を開きます。
3. Firefox ロゴを 5 回連続でタップします。
4. 「設定」に戻り、「Install Extension from File」を開きます。
5. 保存した `.xpi` を選択します。
6. 確認ダイアログで追加します。


### iOS環境またはAndroidのFirefox以外におけるアドオンの入れ方  
原則としてiOSは拡張機能を使用することができません。Safariでは例外でインストールできる場合がありますが、やり方が特殊すぎるためサポートしません。  
もし、どうしても使用したい場合は、QuettaやOrion BrowserなどChromeやFirefoxのアドオン(URLはPC版手順参照)を使えるアプリがあるのでそちらをお試しください。ただし動くかどうかは不明です。  


### 拡張機能の初回設定

1. 拡張インストール後に設定画面を開きます。
2. NImageManager のドメインを入力します。
   - API のフルパスではなく、`https://example.com` のようにドメインだけ入力してください。
3. NImageManager のユーザー名とパスワードでログインします。ここでログインするアカウントは後続の手順で作成してください。
4. `Settings` と `Login` が成功表示になれば準備完了です。

## データ保存場所
DB/画像/派生: `server/data/`  
初期化したい場合はこの中のファイル群をすべて削除してください。  
オリジナルファイルもここに保存されます。


## 使い方

### 画像ファイルのアップロード
画像ファイルを登録するときはアップロード管理画面を使います。ここでは画像ファイル、zipファイル、画像フォルダを取り込めます。

- 画像ファイルまたは zip ファイルをドラッグ＆ドロップする
- 「ファイルを選択」から画像ファイル（単体・複数）または zip ファイルを選ぶ
- 「フォルダを選択」から画像が入ったフォルダを選ぶ


### 監視フォルダによる自動取り込み
監視フォルダへ `png` / `webp` を置くと、アップロードと同じ扱いで自動登録されます。登録作者はマスターアカウントです。使う場合はアプリ直下の `.env` に設定してください。

- `NAI_IM_DROP_IMPORT_ENABLED`：監視フォルダによる自動取り込みを使う設定。`1` で有効、`0` で無効
- `NAI_IM_DROP_IMPORT_DIR`：監視するフォルダの設定。相対パス・絶対パスの両方に対応
- `NAI_IM_DROP_IMPORT_SETTLE_SEC`：ファイル配置後、取り込み開始まで待つ秒数の設定
- `NAI_IM_DROP_IMPORT_MAX_DEPTH`：監視する階層の深さ設定。`1` なら直下と1階層下まで

```env
NAI_IM_DROP_IMPORT_ENABLED=1
NAI_IM_DROP_IMPORT_DIR=./input_image
NAI_IM_DROP_IMPORT_SETTLE_SEC=3
NAI_IM_DROP_IMPORT_MAX_DEPTH=1
```

```env
NAI_IM_DROP_IMPORT_DIR=D:/NImageManager/input_image
```

成功時は元ファイルが元の場所からなくなります。失敗時は元の場所に残ります。
上記の設定は.env修正後、アプリを再起動させてください。  


### 拡張機能からのアップロード
NIMおよび拡張機能側の初期設定を済ませておく必要があります。

- 画像を生成します。生成後、転送ボタン(画像の保存ボタンの横)を押下します。
- Uploadedのメッセージが表示されれば完了です。


### 画像の一覧表示
画像を探すときはプレビュー管理画面を使います。ここでは検索条件の指定、絞り込み、並び替え、表示切り替えができます。

- 作成者、ソフトウェア、開始日、終了日を指定して「検索」を押す
- タグ検索に文字を入れ、候補から「絞り込み」または「除外」を選んで条件を追加し、「検索」を押す
- 左側のカレンダー、ブックマーク、作者、ソフトを押して絞り込む
- 「解除」で左側の絞り込みを外す
- 並び替えを変更する
- 「メインプロンプト重複排除」を切り替える
- グリッド表示とリスト表示を切り替える


### 画像の詳細確認
画像の内容を確認するときは、プレビュー管理画面の一覧で画像を押します。ここではプロンプト確認、タグのコピー、ファイルのダウンロード、作者追加、単体のブックマーク登録ができます。

- 一覧の画像を押して詳細を開く
- 「元ファイル」または「ポーション」を押してダウンロードする
- アーティスト、クオリティ、キャラクター、その他、ネガティブの各項目でタグを確認する
- 各項目の「全コピー」または「全コピー(強調解除)」でタグをコピーする
- 「メタデータ」を開いて保存された情報を確認する
- 「制作者」の横の追加ボタンで、その制作者を左側の作者欄に追加する
- ☆を押して自分のブックマークリストを開き、保存先にチェックを入れる
- 必要なら「リスト作成」で新しいブックマークリストを作る


### 画像の削除
画像を削除するときはプレビュー管理画面を使います。ここでは個別選択、ページ単位の選択、検索結果全体の選択ができます。

- 削除したい画像のチェックボックスにチェックを入れる
- 「ページ一括」で現在表示中の画像をまとめて選ぶ
- 「全対象」で現在の検索結果全体を対象にする
- 「削除」を押す


### ブックマーク登録
ブックマークを付けるときはプレビュー管理画面を使います。ここでは単体登録と複数画像の一括登録・解除ができます。

- 未登録の画像で☆を押すと、既定のブックマークリストへ登録される
- 登録済みの画像で☆を押すとブックマーク画面が開かれ、対象リストへチェックを入れる・外すことで他のブックマークへの登録や解除が行える。
- 複数画像を登録するときは、画像のチェックボックスで対象を選び「一括BM」を押して、各リストの状態を確認する。変更したいリストだけ `☑ / □ / ▣` を切り替える。「保存」を押して、変更したリストだけまとめて反映する
- 同画面や個人設定からブックマークの登録、名前変更、削除等行えます。


### 共有中の作者・共有中のブックマークの登録
他のユーザーが作品やブックマークの一覧を共有しているときに限り、他人の作品やブックマークを参照することが出来ます。プレビュー管理画面の左側から追加します。

- 作者欄の追加ボタンから共有中のユーザーを選び、作者一覧に追加する
- ブックマーク欄の追加ボタンから共有中のブックマークリストを選び、自分の一覧に追加する
- 追加した作者を押すと、その作者の共有作品で絞り込める
- 追加した共有ブックマークを押すと、その共有ブックマークに入っている作品で絞り込める
- 不要になった共有項目は削除ボタンで外す


### アカウントの追加
別の利用者を追加するときはアカウント管理画面を使います。ここでは利用者の追加、発行 URL の取得、不要な利用者の削除ができます。

- ユーザー名と権限を指定して「作成してURL発行」を押す
- 発行された URL を相手に渡す
- 一覧から不要なユーザーを削除する


### 再解析・統計再集計
登録済み画像の解析結果や統計を更新するときはメンテナンス画面を使います。ここでは全件再解析、統計再集計、失敗画像の再試行ができます。

- 「再解析（全件）」を押して登録済み画像を再解析する
- 「統計再集計」を押して統計を作り直す
- 失敗一覧から「再試行」を押して対象画像だけやり直す
- 再解析の対象から外したい画像は「skip」を押す
